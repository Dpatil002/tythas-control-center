import { ComingSoon } from '@/components/shell/coming-soon';

export default function MetaIntegrationsPage() {
  return (
    <ComingSoon
      moduleName="Meta Pixel & Conversions API"
      phaseNumber={6}
      description="Meta (Facebook/Instagram) Pixel & server-side CAPI event tracking will arrive in Phase 6."
    />
  );
}
